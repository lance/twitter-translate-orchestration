# Go Cloud Events Viewer

This is a simple Knative function written in Go, which simply displays a CloudEvent's data.

## Deployment

This function can be deployed to a Knative-enabled cluster with the following command.

```
$ func deploy
```
